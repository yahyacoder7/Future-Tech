<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Processors List</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        /* تعريف المتغيرات للألوان السماوية */
        :root {
            --primary-sky: #0ea5e9;       /* أزرق سماوي مشرق */
            --dark-sky: #0284c7;          /* أزرق أغمق للعناوين */
            --light-bg: #f0f9ff;          /* خلفية سماوية فاتحة جداً */
            --card-bg: #ffffff;
            --text-dark: #334155;
            --text-light: #64748b;
            --border-radius: 12px;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--light-bg);
            color: var(--text-dark);
            padding: 40px 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        h1.page-title {
            text-align: center;
            color: var(--dark-sky);
            margin-bottom: 40px;
            font-size: 2.5rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* شبكة البطاقات */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
        }

        /* تصميم البطاقة */
        .cpu-card {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            box-shadow: 0 4px 6px -1px rgba(14, 165, 233, 0.1), 
                        0 2px 4px -1px rgba(14, 165, 233, 0.06);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border-top: 5px solid var(--primary-sky);
            position: relative;
        }

        .cpu-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(14, 165, 233, 0.2);
        }

        /* رأس البطاقة */
        .card-header {
            padding: 20px;
            background: linear-gradient(to right, #f0f9ff, #ffffff);
            border-bottom: 1px solid #e2e8f0;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .manufacturer-badge {
            font-size: 0.8rem;
            font-weight: bold;
            padding: 4px 10px;
            border-radius: 20px;
            color: white;
            text-transform: uppercase;
        }

        .intel { background-color: #0066cc; } /* لون إنتل المميز */
        .amd { background-color: #ed1c24; }   /* لون AMD المميز */

        .model-name {
            font-size: 1.4rem;
            color: var(--dark-sky);
            font-weight: 700;
        }

        /* تفاصيل المواصفات */
        .specs-list {
            padding: 20px;
            list-style: none;
        }

        .spec-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
            padding-bottom: 8px;
            border-bottom: 1px dashed #e2e8f0;
            font-size: 0.95rem;
        }

        .spec-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }

        .spec-label {
            color: var(--text-light);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .spec-label i {
            color: var(--primary-sky);
            width: 20px;
        }

        .spec-value {
            font-weight: 600;
            color: var(--text-dark);
        }

        /* الزر السفلي */
        .card-footer {
            padding: 15px 20px;
            background-color: #f8fafc;
            text-align: center;
        }

        .btn-details {
            display: inline-block;
            text-decoration: none;
            color: var(--primary-sky);
            font-weight: 600;
            transition: color 0.2s;
        }

        .btn-details:hover {
            color: var(--dark-sky);
        }

    </style>
</head>
<body>

    <div class="container">
        <h1 class="page-title">Available Processors</h1>

        <div class="grid">
            <% cpus.forEach(function(cpu) { %>
                
                <div class="cpu-card">
                    <div class="card-header">
                        <h2 class="model-name"><%= cpu.specs.model_name %></h2>
                        <span class="manufacturer-badge <%= cpu.manufacturer === 'INTEL' ? 'intel' : 'amd' %>">
                            <%= cpu.manufacturer %>
                        </span>
                    </div>

                    <ul class="specs-list">
                        <li class="spec-item">
                            <span class="spec-label"><i class="fas fa-microchip"></i> Cores/Threads</span>
                            <span class="spec-value"><%= cpu.specs.cores %>C / <%= cpu.specs.threads %>T</span>
                        </li>
                        <li class="spec-item">
                            <span class="spec-label"><i class="fas fa-tachometer-alt"></i> Base/Boost</span>
                            <span class="spec-value"><%= cpu.specs.base_clock %> / <%= cpu.specs.boost_clock %> GHz</span>
                        </li>
                        <li class="spec-item">
                            <span class="spec-label"><i class="fas fa-plug"></i> Socket</span>
                            <span class="spec-value"><%= cpu.specs.socket %></span>
                        </li>
                        <li class="spec-item">
                            <span class="spec-label"><i class="fas fa-bolt"></i> TDP</span>
                            <span class="spec-value"><%= cpu.specs.consumption %>W</span>
                        </li>
                        <li class="spec-item">
                            <span class="spec-label"><i class="fas fa-tv"></i> Graphics</span>
                            <span class="spec-value"><%= cpu.specs.integrated_graphics %></span>
                        </li>
                    </ul>

                    <div class="card-footer">
                        <a href="/cpu/<%= cpu.id %>" class="btn-details">View Details <i class="fas fa-arrow-right"></i></a>
                    </div>
                </div>

            <% }); %>
            </div>
    </div>

</body>
</html>